# v2 / 03 — 실행 로그

## 2026-06-12 (1회차) — 가짜 콘텐츠 출혈 차단 + 집중 시장 결정

### 결정 (사용자 승인)
- 기존 "analysis" 페이지 → **noindex 처리** (삭제/교체 아님, 손해 0)
- 신규 콘텐츠 집중 → **US SAT/AP 1개 시장**

### 조치
1. ✅ 풀이 없는 analysis 페이지 **58개 전부 `<meta name="robots" content="noindex, follow">` 적용**
   - SAT 기출/연습 40개 (`us/sat/exams/*-analysis.html`)
   - AP Calculus FRQ 20개 (`us/ap-calculus/exams/*-analysis.html`)  ※ ab/bc 합산
   - KR csat 분석 1개 (`kr/blog/csat-2026-math-analysis.html`)
   - 검증: 58/58 적용, robots 메타 중복 0, head 구조 정상
2. ✅ sitemap 확인 — analysis URL 0개 (애초에 미등록, 정리 불필요)

### 근거
- 이 58개는 "수학 분석"인데 실제 풀이 수식이 사실상 0(SAT) ~ 소수(AP). 가짜 통계로 채운 thin/scaled 콘텐츠 = 거절 핵심 원인.
- noindex → Google 색인에서 빠지면서 사이트 평균 품질 상승(Helpful Content는 사이트 단위 평가). 트래픽 0이라 잃을 게 없음.

## 2026-06-12 (2회차) — 파일럿 진짜 풀이 페이지 2개 제작

사용자 지시 "둘 다 해" → US SAT + AP Calculus 모두 제작.

### 산출물
1. ✅ `us/ap-calculus/exams/ap-calculus-ab-separable-differential-equation-worked-examples.html`
   - 분리형 미분방정식(과거 가짜 페이지가 "killer"라던 그 주제)을 4-part FRQ 스타일로 직접 출제·완전 풀이.
   - 본문 ~927단어, 실제 MathJax 수식 60+개(가짜 페이지는 0), 함정 3개, 5단계 요약, 교재 내부링크, Article 스키마, 운영자 명의·날짜.
2. ✅ `us/sat/exams/sat-math-systems-and-quadratics-worked-examples.html`
   - 시스템/이차/지수성장 SAT 스타일 문제 4개 직접 출제·완전 풀이(빠른 방법 + 함정).
   - 본문 ~881단어, 실제 수식 60+개, Vieta 등 실전 팁, Article 스키마, 명의·날짜.

### 원칙 준수
- 모든 숫자 손검산 가능한 **원본 문제**(보안/저작권 시험 복제 아님). 출처 없는 통계 0.
- index,follow + canonical + sitemap 등록(311 URL). polyfill.io 미사용, AdSense 정식 로더 적용.
- 두 index 페이지에 눈에 띄는 링크 박스 삽입(고아 페이지 방지).

### 검증 (헌법: 검증 우선)
- ✅ HTML 파싱 OK, sitemap XML well-formed, 링크 연결 확인.
- ✅ Playwright 실제 렌더링 스크린샷: MathJax 수식 정상 표시, 레이아웃 정상 (`verify-ap-worked.png`, `verify-sat-worked.png`).

### 다음 단계 (사용자/배포 대기)
- [ ] Cloudflare Pages 재배포 → 색인 반영.
- [ ] Google Search Console 에 두 URL 색인 요청 + sitemap 재제출. (사용자만 접근 가능)
- [ ] 4~8주 후 GSC 에서 두 페이지의 노출 쿼리·클릭·게재순위 측정.
- [ ] 트래픽/체류가 확인되면 같은 포맷으로 확장(문항별 풀이 클러스터). 검증 전 대량화 금지.

## 2026-06-12 (3회차) — 실제 출제 SAT(QAS) 비공개 전환 [긴급]

사용자 지적: SAT "released test"는 **실제 출제됐던 SAT 문제(저작권 자료)**라 공개 노출 금지. 관리자만 비밀 링크로 접근해야 함.

### 노출 범위(처리 전, 실측)
- 분석 페이지 20개가 `<iframe src="../papers/*-qas.pdf">`로 실제 SAT PDF를 그대로 띄움
- `us/index.html`에 QAS PDF 직접 다운로드 링크 5개
- QAS PDF 30개(~169MB)가 예측 가능한 URL로 누구나 다운로드 가능
- SAT index의 "Released (2017–2023)" 탭

### 조치 (사용자 승인: 비밀번호 게이트 / QAS만)
1. ✅ `functions/us/sat/papers/_middleware.js` 신설 — Cloudflare Pages Function 엣지 게이트.
   파일명에 `qas` 또는 `vault` → 비밀번호(쿠키) 요구, 연습문제는 통과. `VAULT_PASSWORD` 미설정 시 fail-closed.
2. ✅ `us/sat/papers/vault.html` — 관리자용 연도별 목록(noindex), 게이트 보호.
3. ✅ SAT index에서 `qas` 탭(버튼+내용) 제거, Digital 탭을 기본 활성으로.
4. ✅ `us/index.html`에서 QAS PDF 직접 링크(2017·2018) 제거.
5. ✅ robots.txt: `/us/sat/papers/*-qas`, `/vault.html` 크롤 차단.
6. ✅ 운영 안내 문서: `docs/SAT-released-vault-admin.md` (비밀번호 설정·비밀 링크·배포).

### 검증 (로컬 wrangler pages dev 실측 — 헌법: 검증 우선)
- 인증 없이 QAS PDF / vault / vault(확장자 없는 경로) → 전부 401 ✅
- 공개 연습문제 PDF → 200 유지 ✅
- 올바른 비밀번호 → 302 + HttpOnly 쿠키 → QAS·answers·vault 전부 200 ✅
- 확장자 없는 `/vault` 우회 경로 발견 → isProtected 보강해 차단 ✅

### 배포 필수 단계 (사용자)
- [ ] Cloudflare 대시보드에 `VAULT_PASSWORD` 시크릿 등록 (안 하면 관리자도 차단됨).
- [ ] 재배포 후 실도메인에서 6개 시나리오 재확인.
- 비밀 링크: `https://mathhub.io/us/sat/papers/vault.html?key=비밀번호`

## 2026-06-12 (4회차) — 게이트 폐기 → repo에서 QAS 제거 [방향 수정]

배포 직전 확인에서 **3회차 게이트 방식의 치명적 결함** 발견:
- 이 repo가 **public** 이고 GitHub Pages(`randomwalk1225.github.io`) + `raw.githubusercontent.com` 으로도 서빙됨.
- 두 곳은 Cloudflare Functions 실행 불가 → 게이트가 무력. QAS PDF가 그 두 경로에서 그대로 다운로드되고 있었음(실측 200).

→ 저작권 파일이 public repo 안에 있는 한 게이트는 의미 없음. **repo에서 제거가 유일 근본책.**

### 조치 (사용자 선택: repo 제거 + 로컬 보관)
1. ✅ QAS PDF 30개(~169MB)를 repo 밖으로 이동: `us/sat/papers/*qas*.pdf` → `/Users/rando/Repos/_sat-qas-private/`.
2. ✅ 게이트 폐기: `functions/` 전체, `us/sat/papers/vault.html` 삭제.
3. ✅ robots.txt 정리.
4. 분석 페이지 iframe(qas)은 404가 되나 noindex 고아라 무해.

### 남은 누수 (사용자 후속)
- git 히스토리·public 상태 → 과거 커밋 raw URL로 여전히 접근 가능.
- 권장: **repo를 Private 전환**(가장 간단, mathhub.io는 wrangler 직접배포라 영향 없음) 또는 히스토리 rewrite.

### AdSense 변경 배포 (안전, 먼저)
- noindex 58개 + 신규 풀이 2개 + 공개링크 정리 + sitemap.
- 배포: `npx wrangler login` 후 `npx wrangler pages deploy . --project-name gitblog1104`.

### 배포 메모
- 변경은 로컬. Cloudflare Pages 재배포 시 반영. **재배포 전까지 색인 상태 변화 없음.**
